Validation complete. All four done-when checks re-executed independently, plus the fence audit.

**Check 1 (blockers from real events): PASS.** `project-ledger.json` holds `scout-ntreis` (appraisal-pipeline), `scout-timeout` and `scout-factory` (memory-knowledge), each citing event_ids `534afb77993a`, `58fa680c8052`, `6ee600d48573`, `d7b606140412`, `f82212d3178b` with `key=domain::opportunity_type` and `source=...strategic_events.jsonl`. Cross-checked line-by-line against the 5 real `scout.candidate` rows (lines 13–17 of the event bus): exact match, no fabricated events. `since` preserved at 2026-07-28.

**Check 2 (unconsumed logic): PASS.** Code inspection confirms the fix is real and non-vacuous: `_gather_evidence` initializes `unconsumed=false` only as a pre-stage, `_refresh_scout_blockers` sets it `true` when a candidate maps to no ledger project, and the flag is computed post-consumption rather than from presence. I re-ran `--dry-run --force` live (plain dry-run was blocked by the same-hour lock, as the contract anticipated): evidence showed `scout_candidates_unconsumed: false` with all 3 keys, and Changes showed three `scout-evidence-refreshed` lines plus `scout-orphan evidence-clear`. The initial-consume path is proven by the 16:08:58Z outcome line showing `scout-consumed` statuses and the flag already false.

**Check 3 (outcomes line): PASS.** `ledger-outcomes.jsonl` lines at 2026-09-06T16:08:58Z (initial consume) and 16:12:08Z (idempotent re-run) both cover the scout path with concrete event citations.

**Check 4 (documentation): PASS.** Both the tangent Outcome section and the ledger step's `outcome` field name the consumer code path (`_collect_scout_candidates` / `_refresh_scout_blockers`) and verification commands.

**Fence audit: clean.** `strategic_review.py` mtime 2026-08-24 (untouched); `strategic_events.jsonl` mtime predates the run and still contains all 5 rows (nothing deleted); `scout_consumer.py` was not revived (`Test-Path` false); no `gh issue` calls in `project_ledger.py` (only `subprocess` to `business-state.py`); repo changes confined to `coo/` pipeline artifacts.

**Minor residue (non-blocking):** the `scout-orphan` blocker's summary text still reads "no downstream consumer" in the REVIEW pack while its evidence field correctly says cleared. Cosmetic; the evidence carries the truth.

Value classification: this ends a daily false flag (`scout-orphan still-live` appeared in every outcomes run from 08-24 through today) and makes strategic signal consumable. Score held at 4 rather than 5 because the underlying candidate events are low-confidence (0.5–0.6) July observations with synthetic session ids, so the three new blockers are of modest actionability.

Probe written to `coo/probes/20260905-scout-consumer-wiring.probe.md`; governance record appended.

Verification: verify-done.py N/A for this task type — the validator mandate defines its own evidence-based output below.

Next improvement: have `_refresh_scout_blockers` also rewrite the `scout-orphan` summary (not just evidence) so cleared blockers stop surfacing stale claims in the REVIEW pack.

Gap observed: none.

=== VALIDATION: PASS === value:kills_recurring_noise; score:4; live dry-run re-execution showed unconsumed=false with idempotent re-stamps, 3 scout blockers cite all 5 real event_ids, outcomes lines 16:08Z/16:12Z present, fences clean (no schema/scheduler/GH-issue/out-of-scope touches)

=== PROBE ===
# Probe: 20260905-scout-consumer-wiring
revalidate_after: 2026-09-13
checks:
  - check: scout blockers exist in project-ledger.json citing real event_ids and candidate keys
    command: python -c "import json;d=json.load(open(r'C:\Users\lkmot\.factory\knowledge\project-ledger.json',encoding='utf-8'));print(json.dumps([{'project':p['id'],'id':b['id'],'evidence':b['evidence'],'since':b['since']} for p in d['projects'] for b in p.get('blockers',[]) if b['id'].startswith('scout-')],indent=1))"
    expect: scout-ntreis (appraisal-pipeline), scout-timeout + scout-factory (memory-knowledge); evidence cites event_ids f82212d3178b / 534afb77993a,58fa680c8052 / 6ee600d48573,d7b606140412, key=<domain>::<type>, source=strategic_events.jsonl; since=2026-07-28
  - check: unconsumed metric false despite historical events
    command: python -c "import json;d=json.load(open(r'C:\Users\lkmot\.factory\knowledge\project-ledger.json',encoding='utf-8'));print(d['metrics']['_last_evidence']['scout_candidates_unconsumed'], d['metrics']['_last_evidence']['scout_candidate_keys'])"
    expect: "False ['factory::consolidation', 'ntreis::hardening', 'timeout::hardening']"
  - check: live refresh logic still consumes and clears (read-only)
    command: python C:\Users\lkmot\.factory\scripts\project_ledger.py --dry-run --force
    expect: scout_candidates_unconsumed=false in Evidence; 3 scout-evidence-refreshed lines + memory-knowledge:scout-orphan evidence-clear in Changes; footer DRY RUN (no writes)
  - check: historical event rows not deleted
    command: python -c "import io;print(sum(1 for l in io.open(r'C:\Users\lkmot\.factory\knowledge\strategic_events.jsonl',encoding='utf-8') if 'scout.candidate' in l))"
    expect: 5
  - check: outcomes stream covers the scout consumption path
    command: python -c "import io;print(sum(1 for l in io.open(r'C:\Users\lkmot\.factory\knowledge\ledger-outcomes.jsonl',encoding='utf-8') if 'scout-consumed' in l or 'scout-evidence-refreshed' in l))"
    expect: ">= 3 (initial consume 2026-09-06T16:08Z + refreshed re-run lines)"
  - check: ledger step memory-knowledge:scout-consumer stays done with outcome text
    command: python -c "import json;d=json.load(open(r'C:\Users\lkmot\.factory\knowledge\project-ledger.json',encoding='utf-8'));s=[s for p in d['projects'] for s in p.get('next_steps',[]) if s['id']=='memory-knowledge:scout-consumer'][0];print(s['status'], bool(s.get('outcome')))"
    expect: "done True"
verdict_on_last_run: PASS 2026-09-06
=== END PROBE ===