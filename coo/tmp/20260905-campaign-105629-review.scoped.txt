Done. The contract executed fully within its fences: diagnosis locked, fix pack drafted, tangent closed — zero sends, zero mutations to any campaign (reads only).

## Outcome

**Dominant hypothesis: copy/template mechanics + presentation** — not deliverability, not list validity alone:
- Live subject carries unsupported `{abtest|...}` syntax (same defect class that stalled control arm 109964 and is now refused by `campaign_create.py` guards), plus an unsupported `{% if %}` conditional in the body.
- HTML + open/click tracking + tracked Gravatar signature link (clickCount 12 vs openCount 15 — clicks are signature artifacts), the exact pattern the fleet's own A/B/C verdict and wave-1 templates moved away from.
- True volume: headline `sentCount` 335 counts initial touches only; `stats_by_id` sums to **1,137 sends** across initial + 4 followups. Reply surface holds exactly 1 row — a mailbox-full DSN. **0 human replies on 1,137 sends** with working infrastructure (1 bounce total).

**Secondary:** audience misfit — national institutional SFR execs (Invitation Homes, Tricon, Amherst…) vs a Trophy Club solo-appraiser offer; 480/778 prospects already inactive. **Contributing:** 15/17 senders still bounced-class fleet-wide (B1), though campaign-local bounces are just 1.

## Artifacts (fleet-triage `coo/outcomes/`)
- `20260905-campaign-105629-diagnosis.md` — memo with locked hypothesis, falsifiers, ranking
- `20260905-campaign-105629-evidence.json` — live read snapshot + mutation audit
- `20260905-campaign-105629-fix-drafts.md` — operator decision pack: pause 105629, retire the surround institutional audience, ship checklist (B1 health, compliance re-sweep, quota alignment)
- `fix-variant-a/b.subject.txt` + `.body.html` — verbatim wave-1/arm-B and arm-C clones (identity + `{{UNSUBSCRIBE_LINK}}`, no `{abtest`, no `{%`)

Tangent `20260905-campaign-105629-review.md` marked `done` with the outcome summary.

## Verification
- All 7 artifacts exist; evidence JSON parses; guard checks pass (one initial check failure was a check bug — variant B's question subject intentionally omits `{{FIRST_NAME}}`, matching live arm-C 109966 verbatim; re-verified PASS).
- `verify-done.py` file checks: 2/2 passed on the two primary artifacts.
- Governance record, decisions.jsonl entry, and parts-bin entry (pb-0073) appended.

**Awaiting your decision (Tier-B/Tier-A, intentionally not executed):** pause 105629; approve any new paused-draft campaign/audience; B1 deliverability path (vendor ticket vs domain rotation).

Next improvement: make the COO loop's campaign-metric reads use `stats_by_id` series sums and `message/get` reply rows instead of headline counters, so autonudge flags carry true volume from day one.
Gap observed: the contract's "prior artifacts" pointers (`morning-batch pool state`, `surround-state.json`) were stale or unrelated to this campaign; scoping had to rebuild audience truth from `surround-queue.json` and live membership evidence instead.
